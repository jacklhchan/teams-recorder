<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="TeamsRecorder.Windows.WinUI.Views.RecordDashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TeamsRecorder.Windows.WinUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer
        AutomationProperties.AutomationId="RecordDashboardScrollViewer"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <Grid MaxWidth="1440" Padding="32,24,32,36" RowSpacing="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="4">
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="錄音" />
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="準備好捕捉每一段重要對話" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Command="{Binding RefreshDevicesCommand}" Content="重新整理裝置" />
                    <Button Command="{Binding StartTestCommand}" Content="測試錄音" />
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" ColumnSpacing="20" RowSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="500" />
                    <ColumnDefinition Width="*" MinWidth="280" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="20">
                    <Border
                        Padding="28"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="16">
                        <StackPanel Spacing="22">
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="6">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Ellipse Width="10" Height="10" Fill="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" />
                                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding StatusText, FallbackValue=準備開始錄音}" />
                                    </StackPanel>
                                    <TextBlock FontSize="38" FontWeight="SemiLight" Text="{Binding ElapsedText, FallbackValue=00:00:00}" />
                                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding PeakText, FallbackValue=等待輸入訊號}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                    <ProgressRing Width="24" Height="24" IsActive="{Binding IsBusy}" />
                                    <Button MinWidth="118" Command="{Binding StartCommand}" Content="開始錄製" Style="{StaticResource AccentButtonStyle}" />
                                    <Button MinWidth="118" Command="{Binding StopCommand}" Content="停止並儲存" />
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="{ThemeResource CardStrokeColorDefaultBrush}" />

                            <StackPanel Spacing="14">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="Teams／系統輸出" />
                                    <TextBlock Grid.Column="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding OutputLevelText, FallbackValue=0.0%}" />
                                </Grid>
                                <Border Height="72" Padding="10" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="10">
                                    <ItemsControl ItemsSource="{Binding OutputWaveformBars}">
                                        <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" Spacing="3" VerticalAlignment="Center" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate><DataTemplate x:DataType="local:WaveformBar"><Border Width="5" Height="{Binding Height}" Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="3" Opacity="{Binding Opacity}" VerticalAlignment="Center" /></DataTemplate></ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="麥克風" />
                                    <TextBlock Grid.Column="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding InputLevelText, FallbackValue=未啟用}" />
                                </Grid>
                                <Border Height="72" Padding="10" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" CornerRadius="10">
                                    <ItemsControl ItemsSource="{Binding InputWaveformBars}">
                                        <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" Spacing="3" VerticalAlignment="Center" /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate><DataTemplate x:DataType="local:WaveformBar"><Border Width="5" Height="{Binding Height}" Background="{ThemeResource SystemFillColorCautionBrush}" CornerRadius="3" Opacity="{Binding Opacity}" VerticalAlignment="Center" /></DataTemplate></ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Padding="22" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="16">
                        <StackPanel Spacing="14">
                            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="來源與裝置" />
                            <Grid x:Name="SourceDeviceGrid" ColumnSpacing="16" RowSpacing="14">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <StackPanel Spacing="6"><TextBlock Text="錄製來源" /><ComboBox DisplayMemberPath="DisplayName" IsEnabled="{Binding IsDeviceSelectionEnabled}" ItemsSource="{Binding CaptureSources}" SelectedItem="{Binding SelectedCaptureSource, Mode=TwoWay}" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding SelectedCaptureSourceDescription}" TextWrapping="Wrap" /></StackPanel>
                                <StackPanel Grid.Column="1" Spacing="6"><TextBlock Text="系統輸出" /><ComboBox DisplayMemberPath="DisplayName" IsEnabled="{Binding IsDeviceSelectionEnabled}" ItemsSource="{Binding RenderEndpoints}" SelectedItem="{Binding SelectedRenderEndpoint, Mode=TwoWay}" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding SelectedRenderDescription}" TextWrapping="Wrap" /></StackPanel>
                                <StackPanel Grid.Row="1" Spacing="6"><TextBlock Text="麥克風（可選）" /><ComboBox DisplayMemberPath="DisplayName" IsEnabled="{Binding IsDeviceSelectionEnabled}" ItemsSource="{Binding CaptureEndpoints}" SelectedItem="{Binding SelectedMicrophoneEndpoint, Mode=TwoWay}" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding SelectedMicrophoneDescription}" TextWrapping="Wrap" /></StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="1" Spacing="6"><TextBlock Text="儲存位置" /><TextBox IsEnabled="{Binding IsDeviceSelectionEnabled}" Text="{Binding OutputFolder, Mode=TwoWay}" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding StorageReadinessText}" TextWrapping="Wrap" /></StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="20">
                    <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="16">
                        <StackPanel Spacing="14">
                            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="自動錄製" />
                            <ToggleSwitch Header="偵測 Teams 會議" IsEnabled="{Binding IsDeviceSelectionEnabled}" IsOn="{Binding IsLocalHeuristicAutoStartEnabled, Mode=TwoWay}" OffContent="關閉" OnContent="開啟" />
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding TeamsAutomaticRecordingStatusText}" TextWrapping="Wrap" />
                            <StackPanel Orientation="Horizontal" Spacing="8"><Button Command="{Binding EnableTeamsAutomaticRecordingCommand}" Content="啟用" /><Button Command="{Binding DisableTeamsAutomaticRecordingCommand}" Content="停用" /></StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="16">
                        <StackPanel Spacing="14">
                            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="即時健康狀態" />
                            <Grid ColumnSpacing="10"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><FontIcon FontSize="20" Foreground="{Binding PrimaryHealthBrush}" Glyph="{Binding PrimaryHealthGlyph}" /><StackPanel Grid.Column="1"><TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding PrimaryHealthTitle}" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding PrimaryHealthDetail}" TextWrapping="Wrap" /></StackPanel></Grid>
                            <Grid ColumnSpacing="10"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><FontIcon FontSize="20" Foreground="{Binding MicrophoneHealthBrush}" Glyph="{Binding MicrophoneHealthGlyph}" /><StackPanel Grid.Column="1"><TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding MicrophoneHealthTitle}" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding MicrophoneHealthDetail}" TextWrapping="Wrap" /></StackPanel></Grid>
                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding AudioHealthSummaryText}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="16">
                        <StackPanel Spacing="10"><TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="快速控制" /><Button Command="{Binding ToggleLocalMicrophoneMuteCommand}" Content="切換錄音麥克風靜音" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding RecordingMicrophoneMuteText}" TextWrapping="Wrap" /><Button Command="{Binding SaveDiagnosticsCommand}" Content="儲存診斷報告" /></StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <Border Grid.Row="2" Padding="18" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="16">
                <Grid ColumnSpacing="16"><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions><FontIcon FontSize="20" Glyph="&#xE946;" VerticalAlignment="Top" /><StackPanel Grid.Column="1" Spacing="3"><TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="錄製狀態" /><TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{Binding ResultText, FallbackValue=新工作階段將顯示在錄音庫。}" TextWrapping="Wrap" /></StackPanel></Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
